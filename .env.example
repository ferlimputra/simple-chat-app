PORT=3000
OLLAMA_BASE_URL=http://127.0.0.1:11434
# Change to the model name available in your Ollama instance (e.g. llama3, llama3.1, mistral).
OLLAMA_MODEL=llama3
# Optional: where to find frontend assets for the server to serve.
STATIC_DIR=public

